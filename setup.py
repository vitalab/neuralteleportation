from setuptools import setup, find_packages


print(find_packages())
setup(name='neuralteleportation',
      version='0.0.1',
      description='Neural network teleportation',
      author='',
      author_email='',
      packages=find_packages()
      )
